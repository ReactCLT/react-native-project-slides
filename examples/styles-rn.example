import { StyleSheet, Text, View } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: bold
  },
  subtitle: {
    fontSize: 15
  }
});

const Header = () => (
  <View>
    <Text style={styles.title}>
      ReactJS Charlotte Meetup
    </Text>
    <Text style={styles.subtitle}>
      An awesome meetup!
    </Text>
  </View> 
);

export default Header;